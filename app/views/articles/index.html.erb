<% @articles.each do |a| %>
  <div class="article">
    <%= a.title %>
  </div>
  <%= link_to "Edit", edit_article_path(a) %>
  <%= link_to "Delete", article_path(a), method: :delete, message: "Are you sure?"%>
<% end %>
<%= link_to "Create Article", "articles/new" %>
